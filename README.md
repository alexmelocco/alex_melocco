# alex_melocco

Repo containing personal projects

## Running and editing the Jekyll Component

Run the following commands to serve the Jekyll blog

- cd blog-src
- bundle install (if not already)
- bundle exec jekyll serve

Rebuilding and release the site

- cd blog-src
- bundle exec jekyll build
